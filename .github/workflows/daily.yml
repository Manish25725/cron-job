name: Daily Job

on:
  workflow_dispatch:

  schedule:
    - cron: "* * * * *"

jobs:
  daily:
    runs-on: ubuntu-latest

    steps:
      - name: Print Daily Message
        run: echo "Daily backup completed"